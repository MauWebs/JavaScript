/* Repite el codigo (x) cantidad de veces */

for (
    // Variable
    contador = 0;
    // Condición
    contador <= 10;
    // Acción
    contador += 1) {
    // Retorna
    console.log(contador);
};